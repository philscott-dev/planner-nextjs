import { theme } from 'theme'
export const EventColors = {
  blue: theme.color.blue[300],
  navy: theme.color.navy[300],
  magenta: theme.color.magenta[300],
  green: theme.color.green[300],
  red: theme.color.red[200],
  purple: theme.color.purple[300],
  orange: theme.color.orange[300],
}
