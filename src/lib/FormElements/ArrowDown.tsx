import styled from '@emotion/styled'
import { FaCaretDown } from 'react-icons/fa'

export default styled(FaCaretDown)`
  position: absolute;
  color: ${({ theme }) => theme.color.white[100]};
  margin-right: 24px;
`
