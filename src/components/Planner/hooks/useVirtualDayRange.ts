import { useState, useEffect } from 'react'
import { addDays, addMonths, addYears } from 'date-fns'
import { PlannerInterval } from '../types'

export default function useVirtualDayRange(
  range: Date[],
  plannerInterval: PlannerInterval,
) {
  return []
}
